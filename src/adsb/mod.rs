//! ADS-B — dump1090 `aircraft.json` parsing + flight/facility correlation heuristic.
//!
//! See issue #8. Parsing/correlation are pure logic over fixtures; the live HTTP feed (#16) is a
//! separate, stubbed concern. **Stub** — implementation pending.
